#spring.application.name=producto
spring.application.name=producto

#spring.datasource.url=jdbc:postgresql://localhost:5432/techstore
#spring.datasource.username=admin
#spring.datasource.password=admin123
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#jwt.secret=miClaveSuperSecretaParaJWT123456789
#jwt.expiration=3600000

# credenciales login
#app.security.username=admin
#app.security.password=admin123

server.port=${SERVER_PORT:8080}

# Base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/techstore}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:admin123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Seguridad
app.security.username=${APP_SECURITY_USERNAME:admin}
app.security.password=${APP_SECURITY_PASSWORD:admin123}

# JWT
jwt.secret=${JWT_SECRET:clave_super_secreta_para_techstore_2026_segura_123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}

aws.sqs.queue-url=${AWS_SQS_QUEUE_URL:}